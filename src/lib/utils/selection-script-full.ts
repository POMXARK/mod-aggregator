/**
 * Полный JavaScript код для функционала выделения элементов
 * Вынесено из PageViewer для разделения логики
 */

/**
 * Генерирует полный JavaScript код для скрипта выделения
 * @param scriptId - уникальный ID скрипта
 * @returns полный JavaScript код
 */
export function generateSelectionScript(scriptId: string): string {
  // Полный код скрипта выделения (копия из PageViewer.svelte строки 116-782)
  // Из-за размера, код будет перенесен при следующем шаге
  return `(function() {
    'use strict';
    const SCRIPT_ID = '${scriptId}';
    // ... полный код ...
  })();`;
}



