# Запуск приложения в браузере

Приложение можно запустить в обычном веб-браузере для разработки и тестирования UI без необходимости компилировать Tauri бэкенд.

## Как запустить

### Вариант 1: Через npm (рекомендуется)
```bash
npm run dev
```

Приложение откроется в браузере по адресу `http://localhost:1420`

### Вариант 2: Через Vite напрямую
```bash
npx vite
```

## Что работает в браузере

✅ **Работает:**
- Весь UI и навигация
- Конструктор парсера (визуальный редактор нод)
- Просмотр страниц (с ограничениями CORS)
- Все компоненты Svelte

⚠️ **Ограничения:**
- Используются mock-данные вместо реальной базы данных
- `fetch_page` возвращает простую HTML-страницу вместо реального контента
- `save_page_local` не сохраняет файлы (только логирует)
- Некоторые команды Tauri недоступны

## Mock данные

В режиме браузера используются следующие mock-данные:

- **Сайты**: 2 примера сайтов
- **Моды**: 2 примера модов для каждого сайта
- **Уведомления**: 1 пример уведомления

Вы можете редактировать mock-данные в файле `src/lib/tauri-mock.ts`

## Индикатор режима

В верхней части приложения отображается баннер:
```
🌐 Режим браузера (Mock данные) - для полного функционала используйте Tauri
```

Этот баннер виден только в браузере, в Tauri приложении он скрыт.

## Переключение между режимами

Приложение автоматически определяет, запущено ли оно в Tauri или в браузере:

- **Tauri**: Использует реальные команды `invoke` из `@tauri-apps/api/core`
- **Браузер**: Использует mock-реализацию из `src/lib/tauri-mock.ts`

Определение происходит через проверку наличия `window.__TAURI__`.

## Отладка

Все mock-вызовы логируются в консоль с префиксом `[MOCK]`:

```javascript
[MOCK] invoke called: get_sites
[MOCK] invoke called: get_mods { siteId: 1 }
```

## Ограничения CORS

В браузере `fetch_page` не может обходить CORS, поэтому возвращается простая HTML-страница. Для реального парсинга используйте Tauri приложение.

## Разработка

Для разработки UI удобнее использовать браузерный режим:
- Быстрый hot-reload
- Удобные DevTools браузера
- Не нужно компилировать Rust код

Для тестирования полного функционала используйте `npm run tauri:dev`.








